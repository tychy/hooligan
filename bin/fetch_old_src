#!/bin/bash

rm -rf oldsrc
mkdir oldsrc
cd oldsrc
git clone git@github.com:tychy/hooligan.git .

if [[ $# -eq 1 ]]; then
    git checkout $1
fi

ls | grep -E -v "*\.c" | xargs rm -rf
rm -rf .git .github .gitignore
cd ..
